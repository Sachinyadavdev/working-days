{
  "name": "@ems/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "type-check": "tsc --noEmit",
    "lint": "eslint src/ --ext .ts"
  },
  "devDependencies": {
    "@ems/eslint-config": "workspace:*",
    "rimraf": "^6.0.0",
    "typescript": "^5.7.0"
  }
}
